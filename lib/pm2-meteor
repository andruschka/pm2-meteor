#!/usr/bin/env node
// Generated by CoffeeScript 1.10.0
(function() {
  var cli, cmds, commands, options;

  cli = require('cli');

  cmds = require('./commandList');

  commands = ['init', 'deploy', 'start', 'stop', 'status', 'generateBundle'];

  options = cli.parse(null, commands);

  cmds[cli.command](options);

}).call(this);
